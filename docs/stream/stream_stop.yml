Stop the streaming of AI Inference
---
tags:
  - stream

parameters:
  - in: path
    name: uuid
    required: true
    schema:
      type: string
        
responses:
  200:
    schema:
      type: string
      description: success message
      example: "Stop stream success ! "
  400:
    schema:
      type: string
      description : error message
      example: "{ error message }"
