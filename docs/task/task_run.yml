Run the task
---
tags:
  - task

parameters:
  - in: path
    name: uuid
    required: true
    schema:
      type: string

responses:
  200:
    schema:
      type: string
      description: success message
      example: "Run Application ( {uuid} ) !"
  400:
    schema:
      type: string
      description : error message
      example: "{ error message }"
